#include <iostream>
using namespace std;

int main() {
    int n;
    string octal = "";

    cout << "Enter decimal number: ";
    cin >> n;

    while(n > 0) {
        octal = char((n % 8) + '0') + octal;
        n /= 8;
    }

    cout << "Octal = " << octal;
    return 0;
}
